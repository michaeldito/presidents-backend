const Utils = require('../../../utils');

/**
 * 
 * 
 */
module.exports = async function(user) {
  // TODO:
  // did the next player play a hand a while back so good
  // that it maybe skipped a player or two and then everyone
  // after the skip turns passed? if so that great turn
  // ended the round
  

  if (1) {
    return Promise.reject(new Error(''));
  }

  return this.save();
}